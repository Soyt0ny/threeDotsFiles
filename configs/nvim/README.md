# Neovim config placeholder

Place your full Neovim configuration in this folder when you are ready.

Suggested approach:

1. Start with `init.lua` and a minimal plugin manager setup.
2. Keep machine-specific secrets or tokens outside this repository.
3. Update `scripts/link.sh` mappings if you choose a custom structure.
