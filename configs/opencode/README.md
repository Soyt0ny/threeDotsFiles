# OpenCode config placeholder

Place your portable OpenCode configuration files here before linking.

Do not store authentication tokens or private credentials in this repository.

If you add files under this directory, ensure `scripts/link.sh` contains the corresponding symlink targets.
